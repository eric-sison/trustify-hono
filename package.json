{
  "name": "trustify-api",
  "scripts": {
    "dev": "bun run --hot src/main.ts",
    "studio": "drizzle-kit studio",
    "migration:generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "migration:amend": "drizzle-kit drop && drizzle-kit generate",
    "migration:drop": "drizzle-kit drop"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.5.9",
    "ioredis": "^5.4.1",
    "postgres": "^3.4.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  }
}
